  local players = game:GetService("Players")
local localplayer = players.LocalPlayer
local camera = workspace.CurrentCamera

local v1,v2,v3,v4,v5

return {
	UNIVERSAL={
		BodyParts = {
			["TORSO"] = "HumanoidRootPart",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if player.Character then
				    local humanoid = player.Character:FindFirstChild("Humanoid")
					if (player.Team ~= localplayer.Team or player.Neutral) and player ~= localplayer and ((humanoid) and humanoid.Health>0 or true) then
						enemies[#enemies+1] = {
							Instance = player.Character,
							Parent = player.Character.Parent,
							Ignore = player.Character,
						}
					end
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1654459251,
			GameName = game.Name,
			GameId = game.GameId,
			PlaceId = game.GameId,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "UNIVERSAL",
		},
	},
	    [2132866904] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,character in next, workspace:GetChildren() do
			    if character.Name=="r15_rig" then
			       enemies[#enemies+1] = {
						Instance = character,
						Parent = character.Parent,
						Ignore = character,
					} 
			    end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return nil
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1655041528,
			GameName = "Frontlines",
			GameId = 2132866904,
			PlaceId = 5938036553,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[1315961587] = {
		BodyParts = {
			["TORSO"] = "humanoid_root_part",
			["HEAD"] = "head",
		},
		Init = function(self)
		    v1=workspace:WaitForChild("characters")
		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,character in next, v1:GetChildren() do
			    if character.Name~="StarterCharacter" then
			        enemies[#enemies+1] = {
    					Instance = character,
    					Parent = character.Parent,
    					Ignore = character,
    				} 
			    end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return v1:FindFirstChild("StarterCharacter")
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1655042242,
			GameName = "Deadline",
			GameId = 1315961587,
			PlaceId = 3837841034,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[1985370796] = {
		BodyParts = {
			["TORSO"] = "HumanoidRootPart",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
			    local character = player.Character
			    if character then
    				if (player.Team ~= localplayer.Team) and player ~= localplayer then
    					enemies[#enemies+1] = {
    						Instance = character,
    						Parent = character.Parent,
    						Ignore = character,
    					}
    				end
			    end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1655037215,
			GameName = "Trenches",
			GameId = 1985370796,
			PlaceId = 5648523896,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[108629575] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and player.Team ~= localplayer.Team) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654712456,
			GameName = "British Military Academy",
			GameId = 108629575,
			PlaceId = 270499015,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[595270616] = {
		BodyParts = {
			["TORSO"] = "HumanoidRootPart",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1655036911,
			GameName = "State of Anarchy",
			GameId = 595270616,
			PlaceId = 2000062521,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[261612318] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654622060,
			GameName = "Fireteam",
			GameId = 261612318,
			PlaceId = 633284182,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[2596968656] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654615560,
			GameName = "Sandy Shores",
			GameId = 2596968656,
			PlaceId = 6830111083,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[580765040] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and player.Team ~= localplayer) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654598380,
			GameName = "Ragdoll Universe",
			GameId = 580765040,
			PlaceId = 1466995005,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[1175300013] = {
    	BodyParts = {
    		["TORSO"] = "UpperTorso",
    		["HEAD"] = "Head",
    	},
    	Init = function(self)
    
    	end,
    	SetCFrame = function(self,CFrame)
    		camera.CFrame = CFrame
    	end,
    	Valid = function(self)
    		return true
    	end,
    	Translate = function(self,text)
    		return self.BodyParts[text:upper()] or ""
    	end,
    	GetIgnorables = function(self)
    		return {}
    	end,
    	GetEnemies = function(self)
    		local enemies = {}
    		for _,player in next, players:GetPlayers() do
    			if (player.Character) and player ~= localplayer then
    				enemies[#enemies+1] = {
    					Instance = player.Character,
    					Parent = player.Character.Parent,
    					Ignore = player.Character,
    				}
    			end
    		end
    		return enemies
    	end,
    	GetLocalCharacter = function(self)
    		return localplayer.Character
    	end,
    	GetLocalPosition = function(self)
    		local character = self:GetLocalCharacter()
    		if character then
    			return character[self:Translate("HEAD")].Position
    		end
    	end,
    	Info = {
    		Created = 1654452304,
    		GameName = "Blacklands",
    		GameId = 1175300013,
    		PlaceId = 3254838662,
    		AntiCheatHash = "N/A",
    		ClientHash = "N/A",
    		GetClientHash = function() return "N/A" end,
    		GetAntiCheatHash = function() return "N/A" end,
    		__TYPE = "API",
    	},
    },
[1168263273] = {
		BodyParts = {
			["TORSO"] = "Chest",
			["HEAD"] = "Head",
		},
		Init = function(self)
		    hookfunction(getgenv().PluginManager,function()return;end)
            for _,table in pairs(getgc(true)) do
                if (typeof(table)=="table") and rawget(table,"SetTeamColor") then
                    v1=table
                    break
                end
            end
            for i,v in pairs(getgc()) do
                if (typeof(v)=="function") and debug.getinfo(v).name=="GetCharacter" then
                    v2=v
                    break
                end
            end
		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return game:GetService("Workspace").Menu.Loadouts:GetChildren()
		end,
		GetEnemies = function(self)
		    local enemies = {}
            local playerlist = debug.getupvalue(v2,1)
            for i,character in pairs(playerlist) do
                local isfriendly = v1:ArePlayersFriendly(i,game.Players.LocalPlayer)
                if not isfriendly then
                    enemies[#enemies+1] = {
						Instance = character.Body,
						Parent = character,
						Ignore = character,
					}
                end
            end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return debug.getupvalue(v2,1)[localplayer]
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654449815,
			GameName = "BadBuisness",
			GameId = 1168263273,
			PlaceId = 3233893879,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[380704901] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and (player.Neutral or player.Team ~= localplayer)) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654447033,
			GameName = "RO-GHOUL",
			GameId = 380704901,
			PlaceId = 914010731,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},

	[2788648141] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and (player.Neutral or player.Team ~= localplayer)) and player ~= localplayer and player.Character:FindFirstChild("Humanoid").Health>0 then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654446617,
			GameName = "Military Tycoon",
			GameId = 2788648141,
			PlaceId = 7180042682,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
    [1390601379] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player) and player.Character and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654609948,
			GameName = "Combat Warriors",
			GameId = 1390601379,
			PlaceId = 4282985734,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[833423526] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return self:GetLocalCharacter() and true or false
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {unpack(workspace.IgnoreThese:GetChildren())}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player~=localplayer and player.Character) and (player.Neutral or player.Team ~= localplayer.Team) then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1635960653,
			GameName = "Strucid",
			GameId = 833423526,
			PlaceId = 2377868063,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[111958650] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return (self:GetLocalCharacter()) and self:GetLocalCharacter():FindFirstChild("Gun") and true or false
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {unpack(workspace.Ray_Ignore:GetChildren()),unpack(workspace.Debris:GetChildren())}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player~=localplayer and player.Character) and (player.Neutral or player.Team ~= localplayer.Team) then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1636009701,
			GameName = "Arsenal",
			GameId = 111958650,
			PlaceId = 286090429,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
		PeriodicChecking = true,
	},
	[2162282815] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {unpack(workspace.Ray_Ignore:GetChildren()),unpack(workspace.Debris:GetChildren())}
		end,
		GetEnemies = function(self)
		    local enemies = {}
			for i,v in pairs(game.Players:GetPlayers()) do
                if v~=localplayer and (v.Neutral or v.Team ~= localplayer.Team) then
                    local character = workspace.MapFolder.Players:FindFirstChild(v.Name)
                    if character then
                        enemies[#enemies+1] = {
                            Instance = character,
                            Ignore = character,
                            Parent = character.Parent
                        }
                    end
                end
			end
        return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1654363142,
			GameName = "Rush Point",
			GameId = 2162282815,
			PlaceId = 5993942214,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
}
