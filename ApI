local players = game:GetService("Players")
local localplayer = players.LocalPlayer
local camera = workspace.CurrentCamera

local v1,v2,v3,v4,v5

return {
	UNIVERSAL={
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if player.Character then
				    local humanoid = player.Character:FindFirstChild("Humanoid")
					if (player.Neutral or player.Team ~= localplayer) and player ~= localplayer and ((humanoid) and humanoid.Health>0 or true) then
						enemies[#enemies+1] = {
							Instance = player.Character,
							Parent = player.Character.Parent,
							Ignore = player.Character,
						}
					end
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1654459251,
			GameName = game.Name,
			GameId = game.GameId,
			PlaceId = game.GameId,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "UNVIERSAL",
		},
	},
	[580765040] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and player.Team ~= localplayer) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654598380,
			GameName = "Ragdoll Universe",
			GameId = 580765040,
			PlaceId = 1466995005,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[1175300013] = {
    	BodyParts = {
    		["TORSO"] = "UpperTorso",
    		["HEAD"] = "Head",
    	},
    	Init = function(self)
    
    	end,
    	SetCFrame = function(self,CFrame)
    		camera.CFrame = CFrame
    	end,
    	Valid = function(self)
    		return true
    	end,
    	Translate = function(self,text)
    		return self.BodyParts[text:upper()] or ""
    	end,
    	GetIgnorables = function(self)
    		return {}
    	end,
    	GetEnemies = function(self)
    		local enemies = {}
    		for _,player in next, players:GetPlayers() do
    			if (player.Character) and player ~= localplayer then
    				enemies[#enemies+1] = {
    					Instance = player.Character,
    					Parent = player.Character.Parent,
    					Ignore = player.Character,
    				}
    			end
    		end
    		return enemies
    	end,
    	GetLocalCharacter = function(self)
    		return localplayer.Character
    	end,
    	GetLocalPosition = function(self)
    		local character = self:GetLocalCharacter()
    		if character then
    			return character[self:Translate("HEAD")].Position
    		end
    	end,
    	Info = {
    		Created = 1654452304,
    		GameName = "Blacklands",
    		GameId = 1175300013,
    		PlaceId = 3254838662,
    		AntiCheatHash = "N/A",
    		ClientHash = "N/A",
    		GetClientHash = function() return "N/A" end,
    		GetAntiCheatHash = function() return "N/A" end,
    		__TYPE = "API",
    	},
    },
	[1168263273] = {
		BodyParts = {
			["TORSO"] = "Chest",
			["HEAD"] = "Head",
		},
		Init = function(self)
		    hookfunction(getgenv().PluginManager,function()return;end)
            for _,table in pairs(getgc(true)) do
                if (typeof(table)=="table") and rawget(table,"SetTeamColor") then
                    v1=table
                    print("done1")
                    break
                end
            end
            for i,v in pairs(getgc()) do
                if (typeof(v)=="function") and debug.getinfo(v).name=="GetCharacter" then
                    v2=v
                    break
                end
            end
		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
		    local enemies = {}
            local playerlist = debug.getupvalue(v2,1)
            for i,character in pairs(playerlist) do
                local isfriendly = v1:ArePlayersFriendly(i,game.Players.LocalPlayer)
                if not isfriendly then
                    enemies[#enemies+1] = {
						Instance = character.Body,
						Parent = character,
						Ignore = character,
					}
                end
            end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654449815,
			GameName = "BadBuisness",
			GameId = 1168263273,
			PlaceId = 3233893879,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[380704901] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and (player.Neutral or player.Team ~= localplayer)) and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654447033,
			GameName = "RO-GHOUL",
			GameId = 380704901,
			PlaceId = 914010731,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},

	[2788648141] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player.Character and (player.Neutral or player.Team ~= localplayer)) and player ~= localplayer and player.Character:FindFirstChild("Humanoid").Health>0 then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654446617,
			GameName = "Military Tycoon",
			GameId = 2788648141,
			PlaceId = 7180042682,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
    [1390601379] = {
		BodyParts = {
			["TORSO"] = "Torso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if player.Character and player ~= localplayer then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Info = {
			Created = 1654609948,
			GameName = "Combat Warriors",
			GameId = 1390601379,
			PlaceId = 4282985734,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[833423526] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return self:GetLocalCharacter() and true or false
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {unpack(workspace.IgnoreThese:GetChildren())}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player~=localplayer and player.Character) and (player.Neutral or player.Team ~= localplayer.Team) then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1635960653,
			GameName = "Strucid",
			GameId = 833423526,
			PlaceId = 2377868063,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[111958650] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return (self:GetLocalCharacter()) and self:GetLocalCharacter():FindFirstChild("Gun") and true or false
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {unpack(workspace.Ray_Ignore:GetChildren()),unpack(workspace.Debris:GetChildren())}
		end,
		GetEnemies = function(self)
			local enemies = {}
			for _,player in next, players:GetPlayers() do
				if (player~=localplayer and player.Character) and (player.Neutral or player.Team ~= localplayer.Team) then
					enemies[#enemies+1] = {
						Instance = player.Character,
						Parent = player.Character.Parent,
						Ignore = player.Character,
					}
				end
			end
			return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1636009701,
			GameName = "Arsenal",
			GameId = 111958650,
			PlaceId = 286090429,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
	[2162282815] = {
		BodyParts = {
			["TORSO"] = "UpperTorso",
			["HEAD"] = "Head",
		},
		Init = function(self)

		end,
		SetCFrame = function(self,CFrame)
			camera.CFrame = CFrame
		end,
		Valid = function(self)
			return true
		end,
		Translate = function(self,text)
			return self.BodyParts[text:upper()] or ""
		end,
		GetIgnorables = function(self)
			return {unpack(workspace.Ray_Ignore:GetChildren()),unpack(workspace.Debris:GetChildren())}
		end,
		GetEnemies = function(self)
		    local enemies = {}
			for i,v in pairs(game.Players:GetPlayers()) do
                if v~=game.Players.LocalPlayer then
                    local character = workspace.MapFolder.Players:FindFirstChild(v.Name)
                    if character then
                        enemies[#enemies+1] = {
                            Instance = character,
                            Ignore = character,
                            Parent = character.Parent
                        }
                    end
                end
			end
        return enemies
		end,
		GetLocalCharacter = function(self)
			return localplayer.Character
		end,
		GetLocalPosition = function(self)
			local character = self:GetLocalCharacter()
			if character then
				return character[self:Translate("HEAD")].Position
			end
		end,
		Tabs = {
			Silent = false,
			WeaponModding = false,
			Visuals = true,
		},
		Info = {
			Created = 1654363142,
			GameName = "Arsenal",
			GameId = 2162282815,
			PlaceId = 5993942214,
			AntiCheatHash = "N/A",
			ClientHash = "N/A",
			GetClientHash = function() return "N/A" end,
			GetAntiCheatHash = function() return "N/A" end,
			__TYPE = "API",
		},
	},
}
